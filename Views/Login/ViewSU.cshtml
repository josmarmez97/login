@{
    ViewData["Title"]="WELCOME SU";
}
@model List<login_martin.Models.Vistas>
<div>
    <h2>Bienvenido SUPER USUARIO</h2>
    <h3>INDEX</h3>
    <a href="Ingresa" class="btn btn-info">Agregar</a>
     
    <table class="table">
        <tr>
            <th scope="col">ID</th>
            <th scope="col">USERID</th>
            <th scope="col">NOMBRE</th>            
            <th scope="col">PRIVILEGIOS</th>
            <th scope="col">CONTRASEÑA</th>
            <th scope="col">OPCIONES</th>
        </tr>
    @foreach (var item in Model)
        {
        <tr>                
            <th scope="col">@item.id</th>
            <th scope="col">@item.userid</th>
            <th scope="col">@item.nombre</th>            
            <th scope="col">@item.privilegio</th>
            <th scope="col">@item.pass</th>
            <th>
                
               
                <form action='Eliminar/@item.id' method='post'>
                <button class="btn btn-danger">Eliminar</button>   
                </form>
                <a href="Detalles/@item.id" class="btn btn-primary">Actualizar</a>
                <!--<a href="Eliminar/@item.id" class="btn btn-danger">X</a>-->
            </th>                                   
        </tr>     
         }    
    </table>

    <br><br>
    <a href="IngresarUsuario" class="btn btn-dark">Salir</a>
</div>